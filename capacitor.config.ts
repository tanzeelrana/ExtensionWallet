import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ExtensionWallet',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
